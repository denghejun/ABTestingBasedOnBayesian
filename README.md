## AB Testing Based On Bayesian

## Install Dependencies
```
pip3 install numpy
pip3 install matplotlib
pip3 install scipy
pip3 install IPython
```

## Run

- Click 'Run' button in PyCharm
- Or run the command in terminal `python3 main.py`